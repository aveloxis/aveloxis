# Exact pins: the versions the tracked docs/_build was built with. The
# snowballstemmer pin must equal the Snowball version of the JS stemmer
# this Sphinx ships (docs/_build/html/_static/english-stemmer.js), or site
# search misses words. scripts/docs_toolchain_pin_test.go checks both.
# Bump them together, then rebuild with README's "Build docs" recipe.
sphinx==9.1.0
sphinx-rtd-theme==3.1.0
myst-parser==5.1.0
Pygments==2.20.0
snowballstemmer==3.0.1
